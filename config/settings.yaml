app_env: local
log_level: INFO

paths:
  data_dir: ./data
  index_dir: ./data/indices

model:
  provider: ollama
  name: qwen2.5:1.5b

ollama:
  embed_model: nomic-embed-text

rag:
  top_k: 4
  chunk_size: 320
  chunk_overlap: 64
  min_score: 0.0
  use_cloud_first: false
  fallback_to_ollama: true
